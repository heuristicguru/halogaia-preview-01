---
import Layout from '../../templates/Layout.astro';
const base = import.meta.env.BASE_URL;
const events = [ { title: "Re‑Wild Yourself", url: base + "retreats/re-wild-yourself/" } ];
---
<Layout title="Events & Dates" description="Calendar of upcoming retreats and experiences.">
  <main class="container section">
    <h1 class="h1">Events</h1>
    <ul class="mt-6 grid gap-6 md:grid-cols-2">
      {events.map(e => (
        <li class="card p-6">
          <h3 class="h3"><a class="no-underline" href={e.url}>{e.title}</a></h3>
          <p class="mt-2">See retreat page for dates.</p>
        </li>
      ))}
    </ul>
  </main>
</Layout>
